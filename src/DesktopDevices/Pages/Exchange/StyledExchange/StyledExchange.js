import styled from 'styled-components';

const StyledExchange = styled.div`
  /* Global Exchange Styles */
  font-size: 12px;
  background-color: ${props => props.theme.colors.background.primary};
`;

export default StyledExchange;
